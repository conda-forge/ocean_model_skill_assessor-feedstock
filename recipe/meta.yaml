{% set name = "ocean-model-skill-assessor" %}
{% set version = "1.0.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ocean-model-skill-assessor-{{ version }}.tar.gz
  sha256: a4aa0a12b459e4af8ac52aa2fa63a9a219eb95c822414fcba699c86dd2929bee

build:
  entry_points:
    - omsa=ocean_model_skill_assessor.CLI:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8
    - setuptools-scm
  run:
    - aiohttp
    - alphashape
    - appdirs
    - cf_pandas
    - cf_xarray
    - cmocean
    - datetimerange
    - extract_model
    - intake
    - intake-axds
    - intake-erddap
    - intake-xarray
    - matplotlib-base
    - numpy
    - pandas
    - pyproj
    - python >=3.8
    - pyyaml
    - requests
    - scipy
    - shapely
    - tqdm
    - xarray
    - xcmocean
    - xgcm

test:
  imports:
    - ocean_model_skill_assessor
  commands:
    - pip check
    - omsa --help
  requires:
    - pip

about:
  home: https://github.com/axiom-data-science/ocean-model-skill-assessor
  summary: A package to fully run the comparison between data and model to assess model skill.
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - kthyng
